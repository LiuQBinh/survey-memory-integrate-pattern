# Survey: Integrating Memory into LLM-Based Agents

Cloned source code of open-source memory frameworks for LLM-based agents. One folder per framework, numbered for reference.

## Objective

**Goal:** Survey and compare **how memory is integrated into memory agents** — integration patterns, APIs, and how agents use memory in practice.

- **Integration patterns:** Where and how the agent reads/writes memory (SDK, tools, RAG, graph API, etc.).
- **Storage & retrieval:** Backends, indexing, and recall semantics (e.g. semantic search, temporal, hierarchical).
- **Agent loop:** How memory is injected into context, updated after turns, and exposed as tools or state.

This repo is the codebase for that survey; findings can be documented per framework or in a separate comparison doc.

## Reference

- **Tham khảo** — Survey paper: [arXiv:2512.13564](https://arxiv.org/pdf/2512.13564), **Table 9**: *Overview of Open-Source Memory Frameworks for LLM-Based Agents*
- **Tham khảo** — Paper list: [Shichun-Liu/Agent-Memory-Paper-List](https://github.com/Shichun-Liu/Agent-Memory-Paper-List)

Table columns: **Fac.** (Factual), **Exp.** (Experiential), **MM.** (Multimodal), **Structure**, **Evaluation**.

## Note table (survey findings)

Khảo sát từng framework, ghi lại integration pattern, storage/retrieval, agent loop. Cập nhật khi survey xong mỗi framework.

| # | Framework | Integration pattern | Storage & retrieval | Agent loop | Notes |
|---|-----------|---------------------|---------------------|------------|-------|
| 1 | A-MEM | **In-process Python SDK.** LLM: OpenAI (SDK) or Ollama (LiteLLM) for metadata + evolution. Vector store: ChromaDB. Embeddings: sentence-transformers local (`all-MiniLM-L6-v2`). No HTTP API out of the box. | ChromaDB (in-memory default; optional `PersistentChromaRetriever`). Metadata: keywords, context, tags from LLM (`analyze_content`). Semantic search via Chroma; evolution links memories (Zettelkasten-style). | **Write:** `add_note(content)` → LLM extracts keywords/context/tags → store in Chroma + `self.memories`. Optional evolution (strengthen/update_neighbor) via LLM when threshold hit. **Read:** `search_agentic(query, k)` or `find_related_memories_raw(query, k)` → inject retrieved memories into chat prompt. Chat app calls these before LLM reply and after important turns to save. | First record. To plug into external chat: use SDK in same process, or add a small Python HTTP service wrapping A-mem. |
| 2 | Acontext | **REST API + Python/TS SDK.** Cloud (api_key) or self-host (base_url + api_key). App: create learning space + session, `learning_spaces.learn(space_id, session_id)`, then `sessions.store_message(session_id, blob)` for each message. Recall: expose **Skill Content Tools** `get_skill`, `get_skill_file` to agent (SDK: `SKILL_TOOLS.format_context(client, skill_ids)`, `to_openai_tool_schema()`, `execute_tool()`). No embeddings for retrieval — tool-based, agent picks by name. | API: Go + Gin + PostgreSQL + Redis + RabbitMQ + S3. CORE: Python + FastAPI + PostgreSQL + pgvector + Redis + RabbitMQ + S3. Skills = Markdown files (schema in SKILL.md); stored as artifacts (S3). Metadata in DB; retrieval by skill name + file path, full files not chunks. | **Write:** Session messages → task extraction (from stream/outcome) → on task complete/failed, RabbitMQ triggers **distillation** (LLM: skip_learning / report_success_analysis / report_factual_content) → **Skill Agent** (LLM) writes/updates Markdown via create_skill, str_replace_skill_file, etc. **Read:** Agent calls `get_skill(skill_name)` then `get_skill_file(skill_name, file_path)`; preload skill IDs in context. | Agent Skills as memory: learns from *what the agent did* (task outcomes), not just chat. Export skills as ZIP; use in any framework. |
| 3 | AgentMemory | **In-process Python SDK only.** No HTTP API, no tool pool. App imports `agentmemory` and calls `create_memory(category, text, metadata)`, `search_memory(category, search_text, n_results, filter_metadata)`, `get_memory(category, id)`, `update_memory`, `delete_memory`, etc. Chat app: call `search_memory()` before LLM to inject context; call `create_memory()` (or `create_unique_memory()` for dedup) after important turns. | **ChromaDB** (default, `PersistentClient` at `STORAGE_PATH`) or **Postgres/pgvector** via `CLIENT_TYPE=POSTGRES` + `POSTGRES_CONNECTION_STRING`. Pluggy client factory (`client.py`). Chroma uses default embedding when documents/query_texts passed without embeddings; Postgres uses ONNX `all-MiniLM-L6-v2` via `check_model`/`infer_embeddings` (`check_model.py`). Category = collection; metadata filter, distance/min/max, `novel` flag. | **Write:** App calls `create_memory(category, text, metadata)` or `create_unique_memory(category, content, similarity=0.95)`. **Read:** App calls `search_memory(category, search_text, n_results, ...)` or `get_memories(category, ...)` / `get_memory(category, id)` and injects results into prompt. No built-in agent loop or LLM tools — app owns when to read/write. | Simple vector memory: category-based, semantic search, optional dedup. Export/import JSON (`persistence.py`). Events/epochs for temporal grouping (`events.py`). |
| 4 | Chroma | **Vector DB / search engine, not an agent-memory framework.** Python (`chromadb`) and JavaScript (`chromadb` npm). In-process: `Client()` / `EphemeralClient()` / `PersistentClient(path)`; or **HttpClient(host, port)** / **CloudClient(api_key)** for server/cloud. API: `client.create_collection(name)` or `get_or_create_collection`, then `collection.add(documents=..., metadatas=..., ids=...)`, `collection.query(query_texts=..., n_results=...)`, `collection.get(ids=...)`. No tool pool, no "memory" abstraction — app uses Chroma as store and wires add/query into its own loop. | In-memory (EphemeralClient), on-disk (PersistentClient), or remote (HttpClient to Chroma server; CloudClient to api.trychroma.com). Embedding: pass `embedding_function` to collection (default Sentence Transformers; or OpenAI, Cohere, etc. — many in `chromadb.utils.embedding_functions`) or supply own embeddings. Metadata/where/where_document filters. | No built-in agent loop. Use case: add documents → query relevant docs → compose into LLM context. App calls `collection.add()` when storing; `collection.query(query_texts=[...], n_results=k)` when retrieving; inject results into prompt. Integrations: LangChain, LlamaIndex. | "Chat your data" pattern. Same API in dev and prod. Chroma Cloud = hosted; self-host = `chroma run --path /chroma_db_path`. |
| 5 | Cognee | **Python SDK (in-process) + FastAPI + MCP.** SDK: `cognee.add(data)`, `cognee.cognify()`, `cognee.memify()`, `cognee.search(query_text, query_type)`. API: versioned routes `/add`, `/cognify`, `/search`, `/memify`, `/delete`. **MCP server** (cognee-mcp): tools `cognify`, `save_interaction`, `search`, `list_data`, `delete`, `prune`, `cognify_status` — agent calls these to ingest, build graph, and query. MCP can run stdio/SSE/HTTP; optional `--api-url` to use Cognee API. LLM: multiple providers (OpenAI, Anthropic, Gemini, Ollama, etc.) for entity extraction (Instructor), summarization, and completion search. | **Graph DB** (Kuzu default, Neo4j, Neptune): nodes = DataPoints (entities), edges = relationships; built by cognify (LLM extracts from chunks). **Vector DB** (LanceDB default, Chroma, PGVector): embeddings for chunks and graph; semantic search. **Relational** (SQLite/Postgres): datasets, data items, users, permissions. **Retrieval:** many SearchTypes — GRAPH_COMPLETION (graph + LLM), RAG_COMPLETION, CHUNKS, SUMMARIES, TRIPLET_COMPLETION, CYPHER, TEMPORAL, CODING_RULES, FEELING_LUCKY. **memify:** enrichment pipeline (extraction + enrichment tasks) on graph, not consolidation in the survey sense. | **Write:** `add(data)` → ingest into dataset; `cognify()` → classify → chunk → LLM extract entities/relations → summarize → `add_data_points` (graph + vector). Optional `memify()` to enrich graph. MCP: `cognify(data)` = add + cognify; `save_interaction(data)` = add (node_set user_agent_interaction) + cognify + optional coding rule associations. **Read:** `search(query_text, query_type)` or MCP `search(search_query, search_type, top_k)`; app/agent injects results into prompt. No built-in auto-save per turn — app/MCP decides when to add/cognify/search. | Knowledge graph + vector hybrid (GraphRAG-style). Multimodal ingestion (text, PDF, images, audio, code). MCP exposes Cognee as tools for any MCP-capable agent. Table 9: Fac.+Exp., MM., structure "knowledge graph". |
| 6 | HindSight | **REST API + Python/TS/Rust clients.** `client.retain(bank_id, content)`, `client.recall(bank_id, query)`, `client.reflect(bank_id, query)`. Optional **LLM Wrapper** (2 lines) for auto store/retrieve on LLM calls. Docker or external PostgreSQL. MCP server. LLM: OpenAI, Anthropic, Gemini, Groq, Ollama, LM Studio. | **PostgreSQL + pgvector.** Banks = isolated memory stores. World facts, experience facts, mental models (consolidated). **Retain:** fact extraction (LLM), entity/link creation, chunk storage. **Recall:** 4 parallel strategies (semantic, BM25, graph, temporal) + cross-encoder rerank + fusion. **Reflect:** disposition-aware response (LLM). Consolidation pipeline. | **Write:** `retain(bank_id, content)` → extract facts → store. **Read:** `recall(bank_id, query)` → multi-strategy retrieval → rerank; `reflect(bank_id, query)` → disposition-aware LLM response. App or wrapper decides when to retain/recall/reflect. | SOTA LongMemEval. Biomimetic structures. Paper: arxiv 2512.12818. |
| 7 | LangMem | **LangGraph integration.** `create_manage_memory_tool(namespace)`, `create_search_memory_tool(namespace)` — agent uses as tools. Store = LangGraph **BaseStore** (InMemoryStore or AsyncPostgresStore with embed index). Optional **background memory manager** (extract, consolidate, update). Python; env: LLM API key. | Store-agnostic core API; native **LangGraph Store** (vector index, e.g. 1536 dims, openai embed). Background: extract/consolidate/update. Hot path: agent calls manage + search tools. | **Write:** Agent invokes manage_memory (record/search) during chat; or background manager extracts/consolidates. **Read:** Agent invokes search_memory; or hot-path preload. App passes store into agent; agent decides when to store/search. | Core API works with any storage; default is LangGraph platform store. |
| 8 | LightMem | **Python SDK + MCP server.** `LightMemory(config)` with pre_compress, topic_segment, memory_manager (openai/deepseek/ollama/vllm), text_embedder, embedding_retriever (qdrant). LongMemEval, LoCoMo scripts. | **Qdrant** (default) for embeddings + summaries; optional LLMLingua compression, topic segmentation. LLM for memory update/summary. Modular: PreCompressor, TopicSegmenter, MemoryManager, EmbeddingRetriever. | **Write:** App feeds messages; LightMem extracts/compresses/segments and updates memory. **Read:** Retrieval by embedding or summary; app injects into prompt. MCP exposes tools for agent use. | Lightweight, efficient. ICLR 2026. StructMem variant. |
| 9 | Mem0 | **Python + JS SDK; hosted or self-hosted.** `Memory(config)` → `memory.add(messages, user_id)`, `memory.search(query, user_id, limit)`. LLM required (default gpt-4.1-nano). Optional graph_store. Vector store pluggable. | **Vector store:** Qdrant (default), Chroma, PGVector, Pinecone, Redis, etc. (factory). **History:** SQLite or configurable. Optional **graph** (Mem0g) for entities/relations. LLM extracts/updates facts; reranker optional. Multi-level: user, session, agent. | **Write:** `add(messages, user_id)` → LLM extracts → store in vector (+ graph if enabled). **Read:** `search(query, user_id)` → vector search → return results; app injects into prompt. Chat loop: search before LLM, add after turn. | +26% vs OpenAI Memory (paper); 91% faster, 90% fewer tokens. Hosted: app.mem0.ai. |
| 10 | Memary | **Python SDK (Streamlit app).** `ChatAgent` with memory_stream, entity_knowledge_store, personas. Tools: search, vision, locate, stocks. FalkorDB or Neo4j. | **Graph:** FalkorDB / Neo4j. Memory stream JSON, entity knowledge store JSON. Personas (system, user). LLM (Ollama or OpenAI) + vision. | **Write:** Agent conversation updates memory stream and entity store via graph. **Read:** Search over graph/knowledge; inject into agent context. App owns loop; agent uses tools. | Emulates human memory; multi-graph for multi-agent. |
| 11 | MemEngine | **Python library.** Unified 3-level framework: config (MemoryConfig), memory classes (e.g. FUMemory), store/recall interface. Pluggable backends. LangChain-compatible. | Modular: operations, functions, storage. Implementations from research (FU, etc.). Config-driven; default or configurable mode. | **Write:** `memory.store(...)`. **Read:** `memory.recall(observation)`. App wires memory into agent (e.g. DialogueAgent); app owns when to store/recall. | Library for building custom memory models; TheWebConf'25. |
| 12 | MemGPT / Letta | **Letta platform (successor).** REST API + Python/TS SDK. `client.agents.create(model, memory_blocks, tools)`, `client.agents.messages.create(agent_id, input)`. Memory blocks = human/persona etc. Cloud-hosted; no open-source server in repo. | Memory managed by Letta cloud. memory_blocks in agent config. No direct vector DB in repo — platform abstraction. | **Write/Read:** Platform handles memory; developer sends messages and gets responses. Agent state and memory server-side. | Repo is Letta (stateful agents); MemGPT was open-source predecessor. |
| 13 | Memobase | **REST API + Python/TS/Go SDK + MCP.** User profile-based. Users, insert data (chats), get memory (profile + timeline). Buffer per user; batch processing; no "agents" in pipeline. | **User profile** (structured JSON) + **event timeline** (fine-grained gist). SQL + embeddings. Low online latency (<100ms); prompt template for context API. | **Write:** Insert conversation data; backend buffers and runs fixed 3-step LLM workflow to update profile/timeline. **Read:** `context` API returns packed memories for prompt; or search. | LOCOMO SOTA; 40–50% fewer LLM calls (v0.0.40). memobase.io. |
| 14 | Memori | **Cloud API + Python/TS SDK.** `Memori().llm.register(client).attribution(user_id, agent_id)`; then use LLM as usual — conversations persisted and recalled automatically. | Memori Cloud stores and recalls; LLM-agnostic. Attribution scopes memory per user/agent. | **Write/Read:** Automatic via wrapped LLM client; no explicit add/search in app code. | Zero-config; get API key, register LLM, set attribution. memorilabs.ai. |
| 15 | MemoryOS | **Python SDK + MCP (MemoryOS-MCP).** Hierarchical: short/mid/long-term persona memory. ChromaDB or built-in. Multiple LLMs (OpenAI, DeepSeek, Qwen…). Modules: Storage, Updating, Retrieval, Generation. | Storage engine pluggable; ChromaDB supported. Four modules: Storage, Updating, Retrieval, Generation. User profile + knowledge update. | **Write:** Feed interactions; memory layer updates storage. **Read:** Retrieve and assemble context; inject into prompt. MCP tools for Claude/Cline/Cursor. | LoCoMo +49.11% F1, +46.18% BLEU-1. EMNLP 2025. |
| 16 | MemOS | **REST API + MCP + Cloud.** Unified memory API: add, retrieve, edit, delete. Graph-structured, multi-modal (text, images, tool traces, personas). MemScheduler for async ingestion. Memory feedback & correction. | **Tree memory + memcube.** Multi-cube KB. Graph, inspectable. Redis Streams + DB. v2: KB, feedback, deletion, tool memory, streaming chat. | **Write:** Add memories/chat; async via MemScheduler. **Read:** Retrieve with filters; chat API with memory-aware retrieval. MCP for tools. | MemOS 2.0 Stardust. memos.openmem.net. |
| 17 | MemU | **Python SDK.** Memory as file system: categories = folders, memory items = files. `MemoryService`: memorize, retrieve, CRUD. Backends: inmemory, sqlite, postgres. 24/7 proactive; user intention capture. | **Storage:** inmemory, sqlite, or postgres. Hierarchical categories. LLM for extraction; profile-based routing (default, embedding). Workflow-based (memorize, retrieve). | **Write:** `memorize(...)` flow. **Read:** `retrieve(...)` flow. Proactive agent can run in background. Integrations (e.g. memUBot). | File-system metaphor; 24/7 proactive. memu.pro. |
| 18 | MineContext | **Desktop app + backend.** Proactive context-aware partner. Screenshots, multimodal (documents, images, etc.). Local-first; optional local AI. Context engineering lifecycle: capture → process → store → retrieve → consume. | Local storage; six types of intelligent context. Not a classic agent SDK — desktop/extension app that surfaces context to AI. | **Write:** User recording/capture; backend processes and stores. **Read:** Proactive delivery (summaries, todos) and resurfacing in creation. | Volcengine; context engineering + ChatGPT Pulse–style. |
| 19 | MIRIX | **Python SDK + Cloud or local server.** `MirixClient(api_key, base_url)`. `client.add(user_id, messages)`, search/get via API. `initialize_meta_agent(provider)`. Six memory types (Core, Episodic, Semantic, Procedural, Resource, Knowledge). | **PostgreSQL** + vector; BM25 + vector search. Multi-modal (text, images, voice, screen). Privacy-first local option. | **Write:** `add(user_id, messages)` → pipeline to memory types. **Read:** Search/retrieve; inject into agent. Multi-agent memory system. | mirix.io; 0.1.6+ = pure memory system, pluggable. |
| 20 | Pinecone Assistant MCP | **MCP server only.** Retrieves from Pinecone Assistant. Docker or cargo. Env: PINECONE_API_KEY, PINECONE_ASSISTANT_HOST. No write path in MCP — bridge to Pinecone Assistant. | Pinecone Assistant (hosted); MCP only exposes retrieval to client. | **Read:** MCP tool to retrieve from Assistant. App/agent calls MCP; no memory write in this repo. | Thin MCP adapter for Pinecone Assistant. |
| 21 | PowerMem | **Python SDK + MCP + HTTP API server.** `Memory(config)`, `memory.add(content, user_id)`, `memory.search(query, user_id)`. auto_config() from .env. Ebbinghaus forgetting curve; user profile; multi-agent (scope). | **Hybrid:** vector + full-text + graph. OceanBase or compatible. Sub-stores; intelligent extraction (LLM), dedup, merge. Multi-modal (text, image, audio → text). | **Write:** `add()` → LLM extract → store with forgetting curve. **Read:** `search()` → hybrid retrieval. MCP/API for agent use. | LOCOMO 78.70% accuracy; 96.53% token reduction. |
| 22 | ReMe | **Python (reme-ai).** **ReMeLight:** file-based — MEMORY.md, daily YYYY-MM-DD.md, tool_result cache. `check_context`, `compact_memory`, `summary_memory`, `memory_search`. Vector + BM25. AgentScope integration. | **File-based:** Markdown files; editable. **Vector:** embedding cache; hybrid retrieval. Compactor, Summarizer, ToolResultCompactor. | **Write:** `summary_memory` persists to files; `compact_memory` compresses history. **Read:** `memory_search`; pre_reasoning_hook runs compact/check/summary. | Remember Me, Refine Me. CoPaw uses ReMeLight. |
| 23 | Second Me | **Train "AI self" (personal AI identity).** Local training; HMM (Hierarchical Memory Modeling), Me-Alignment. Docker or integrated setup. Not a generic agent memory SDK — full stack for cloning user identity. | Local model + memory; decentralized network. Identity, context, reflection. | **Write/Read:** Training and inference over user's memories; output is "Second Me" agent. | second.me; AI-native memory papers. |
| 24 | SimpleMem | **Python + MCP.** Efficient lifelong memory; semantic lossless compression. Store, compress, retrieve. Works with Claude Desktop, Cursor, LM Studio, Cherry Studio, any MCP client. | Storage and retrieval with compression. MCP server exposes tools. | **Write:** Store via tool/SDK. **Read:** Retrieve via tool/SDK; inject into prompt. | aiming-lab; arXiv 2601.02553. |
| 25 | SuperMemory | **API + Web app + MCP + Plugins (Claude Code, OpenClaw…).** Single API: memory, RAG, user profiles, connectors (Google Drive, Notion, …). #1 LongMemEval, LoCoMo, ConvoMem. | Memory + RAG + user profiles in one structure. Extracts facts, handles contradictions, forgetting. Hybrid search. Connectors sync. Multi-modal extractors. | **Write:** Conversations and uploads; auto-extract and update. **Read:** User profile (~50ms), hybrid search; one call. App or plugin uses API. | supermemory.ai; research lab. |
| 26 | Weaviate | **Vector DB (not agent-memory framework).** Go. REST/GraphQL/gRPC. Client libs (Python, JS, Go…). `collections.create`, `add`, `query`. Vectorizer modules (OpenAI, Cohere, …) or self-provided embeddings. | Vector DB: HNSW/flat/dynamic index; LSM store; BM25. Multi-tenancy, replication, RBAC. | No built-in agent loop. App adds documents, queries, injects into LLM context. RAG/semantic search use case. | weaviate.io; like Chroma at scale. |
| 27 | Zep | **Zep Cloud + Python/TS/Go SDK.** Add context (chat, events); Zep builds **temporal knowledge graph** (Graph RAG). Retrieve pre-formatted context blocks. Sub-200ms. | **Temporal knowledge graph;** relationship-aware; evolution over time. Cloud-managed. | **Write:** Feed messages/events to Zep. **Read:** Retrieve/assemble context for LLM. App integrates SDK; Zep handles extraction and retrieval. | End-to-end context engineering. getzep.com. |

### Acontext: flow ↔ code mapping

Paths below are under `2-acontext/`. MQ: exchange `session.message` / `learning.skill`; routing keys in `src/server/core/acontext_core/service/constants.py` (EX, RK).

**STORE (message → task → skill)**

| Step | Mô tả | Code |
|------|--------|------|
| 1 | App gửi message | SDK: `src/client/acontext-py/src/acontext/resources/sessions.py` → `store_message()` (L222–316). Gửi **POST** `/session/{session_id}/messages` với `blob`, `format`. |
| 2 | API nhận, lưu DB, publish MQ | Handler: `src/server/api/go/internal/modules/handler/session.go` → `StoreMessage()` (L316). Service: `src/server/api/go/internal/modules/service/session.go` → `StoreMessage()` (L277): lưu message (L369), `PublishJSON(SessionMessage, SessionMessageInsert, {ProjectID, SessionID, MessageID})` (L378–384). |
| 3 | CORE: consumer message mới | `src/server/core/acontext_core/service/session_message.py` → `insert_new_message()` (L43–50, L49 body = InsertNewMessage). Queue: `session.message.insert`, queue name `session.message.insert.entry`. |
| 4 | Buffer / timer hoặc process | Cùng file: buffer chưa đủ → `check_buffer_timer_or_set` + `waiting_for_message_notify` (L75–95) publish `session.message.buffer.process`. Lock không lấy được → publish `session.message.insert.retry` (L109–114). Khi đủ hoặc từ retry: `buffer_new_message()` (L155–204) → lấy lock, gọi `MC.process_session_pending_message`. |
| 5 | Task Agent (extract task, có thể finish) | `src/server/core/acontext_core/service/controller/message.py` → `process_session_pending_message()` (L15–101). Gọi `AT.task_agent_curd()` (L74). Task agent: `src/server/core/acontext_core/llm/agent/task.py` → `task_agent_curd()` (L122–303); tools trong `llm/tool/task_lib/` (insert, update, append, progress, finish). |
| 6 | Publish skill learning khi task xong | Cùng file task.py: khi tool cập nhật task có `learning_task_ids`, publish `SkillLearnTask` (L257–274): `publish_mq(EX.learning_skill, RK.learning_skill_distill, SkillLearnTask(...))`. |
| 7 | Distillation consumer | `src/server/core/acontext_core/service/skill_learner.py` → `process_skill_distillation()` (L25–75). Queue: `learning.skill.distill`. Gọi `SLC.process_context_distillation()` (L51); controller: `src/server/core/acontext_core/service/controller/skill_learner.py` → `process_context_distillation()` (L26–…), LLM prompt: `llm/prompt/skill_distillation.py`, tools: `llm/tool/skill_learner_lib/distill.py`. Sau đó `publish_mq(..., RK.learning_skill_agent, distilled_payload)` (L68–72). |
| 8 | Skill Agent consumer | Cùng file skill_learner.py → `process_skill_agent()` (L84–…). Queue: `learning.skill.agent`. Lock Redis learning space, gọi `SLC.run_skill_agent()`; agent: `src/server/core/acontext_core/llm/agent/skill_learner.py` → `skill_learner_agent`, tools: `llm/tool/skill_learner_lib/` (create_skill, str_replace_skill_file, get_skill, get_skill_file, …). Ghi artifact qua service data/artifact. |

**RECALL (đọc skill trong chat)**

| Step | Mô tả | Code |
|------|--------|------|
| 1 | Preload skills (context) | SDK: `src/client/acontext-py/src/acontext/agent/skill.py` → `SkillContext.create(client, skill_ids)` (L35–57): với mỗi `skill_id` gọi `client.skills.get(skill_id)` → **GET** `/agent_skills/{skill_id}`. Định nghĩa: `src/client/acontext-py/src/acontext/resources/skills.py` → `get()` (L95–105). |
| 2 | Tool schema + context prompt | `skill.py` → `BaseToolPool.to_openai_tool_schema()` (base: `src/client/acontext-py/src/acontext/agent/base.py` L107–108), `ctx.get_context_prompt()` (L20–32: XML `<available_skills>`). |
| 3 | App gửi LLM với tools + prompt | Doc: `docs/content/docs/(guides)/tool/(features)/skill_tools.mdx` (L22–58): loop chat, `tools=SKILL_TOOLS.to_openai_tool_schema()`, system có `skills_context`. |
| 4 | Thực thi tool khi LLM gọi | `skill.py` → `GetSkillTool.execute()` (L188–214), `GetSkillFileTool.execute()` (L283–319). `get_skill`: `ctx.get_skill(skill_name)` (đã có trong context). `get_skill_file`: `ctx.client.skills.get_file(skill_id=skill.id, file_path=...)` → **GET** `/agent_skills/{skill_id}/file?file_path=...` (skills.py L115–141). |
| 5 | API phục vụ skill / file | API (Go): routes trong `src/server/api/go/internal/router/router.go` (learning_spaces, agent_skills); skill file content/URL từ DB + S3. |

**MQ constants:** `src/server/core/acontext_core/service/constants.py` — `EX.session_message`, `EX.learning_skill`; `RK.session_message_insert`, `RK.session_message_insert_retry`, `RK.session_message_buffer_process`, `RK.learning_skill_distill`, `RK.learning_skill_agent`.

## Frameworks

| # | Folder | Framework | GitHub | Website / Docs |
|---|--------|-----------|--------|----------------|
| 1 | `1-a-mem` | A-MEM | [agiresearch/A-mem](https://github.com/agiresearch/A-mem) | — |
| 2 | `2-acontext` | Acontext | [memodb-io/Acontext](https://github.com/memodb-io/Acontext) | — |
| 3 | `3-agentmemory` | AgentMemory | [elizaOS/agentmemory](https://github.com/elizaOS/agentmemory) | [Website](https://www.getzep.com/product/agent-memory/) |
| 4 | `4-chroma` | Chroma | [chroma-core/chroma](https://github.com/chroma-core/chroma) | [trychroma.com](https://www.trychroma.com/) |
| 5 | `5-cognee` | Cognee | [topoteretes/cognee](https://github.com/topoteretes/cognee) | [cognee.ai](https://www.cognee.ai/) |
| 6 | `6-hindsight` | HindSight | [vectorize-io/hindsight](https://github.com/vectorize-io/hindsight) | — |
| 7 | `7-langmem` | LangMem | [langchain-ai/langmem](https://github.com/langchain-ai/langmem) | [Docs](https://langchain-ai.github.io/langmem/) |
| 8 | `8-lightmem` | LightMem | [zjunlp/LightMem](https://github.com/zjunlp/LightMem) | — |
| 9 | `9-mem0` | Mem0 | [mem0ai/mem0](https://github.com/mem0ai/mem0) | [mem0.ai](https://mem0.ai/) |
| 10 | `10-memary` | Memary | [kingjulio8238/Memary](https://github.com/kingjulio8238/Memary) | [Docs](https://kingjulio8238.github.io/memarydocs/) |
| 11 | `11-memengine` | MemEngine | [nuster1128/MemEngine](https://github.com/nuster1128/MemEngine) | — |
| 12 | `12-memgpt` | MemGPT | [cpacker/MemGPT](https://github.com/cpacker/MemGPT) | [docs.letta.com](https://docs.letta.com/) |
| 13 | `13-memobase` | Memobase | [memodb-io/memobase](https://github.com/memodb-io/memobase) | [memobase.io](https://www.memobase.io/) |
| 14 | `14-memori` | Memori | [MemoriLabs/Memori](https://github.com/MemoriLabs/Memori) | [memori.ai](https://memori.ai/) |
| 15 | `15-memoryos` | MemoryOS | [BAI-LAB/MemoryOS](https://github.com/BAI-LAB/MemoryOS) | [baijia.online/memoryos](https://baijia.online/memoryos/) |
| 16 | `16-memos` | MemOS | [MemTensor/MemOS](https://github.com/MemTensor/MemOS) | [memos.openmem.net](https://memos.openmem.net/) |
| 17 | `17-memu` | MemU | [NevaMind-AI/memU](https://github.com/NevaMind-AI/memU) | [memu.pro](https://memu.pro/) |
| 18 | `18-minecontext` | MineContext | [volcengine/MineContext](https://github.com/volcengine/MineContext) | [Website](https://app.daily.dev/posts/minecontext-is-your-proactive-context-aware-ai-partner-b0fvvtds3) |
| 19 | `19-mirix` | MIRIX | [Mirix-AI/MIRIX](https://github.com/Mirix-AI/MIRIX) | [mirix.io](https://mirix.io/) |
| 20 | `20-pinecone-assistant-mcp` | Pinecone | [pinecone-io/assistant-mcp](https://github.com/pinecone-io/assistant-mcp) | [Docs](https://docs.pinecone.io/guides/assistant/overview) |
| 21 | `21-powermem` | PowerMem | [oceanbase/powermem](https://github.com/oceanbase/powermem) | — |
| 22 | `22-reme` | ReMe | [agentscope-ai/ReMe](https://github.com/agentscope-ai/ReMe) | [memoryscope.ai](https://www.memoryscope.ai/) |
| 23 | `23-second-me` | Second Me | [mindverse/Second-Me](https://github.com/mindverse/Second-Me) | [second.me](https://home.second.me/) |
| 24 | `24-simplemem` | SimpleMem | [aiming-lab/SimpleMem](https://github.com/aiming-lab/SimpleMem) | [SimpleMem Page](https://aiming-lab.github.io/SimpleMem-Page/) |
| 25 | `25-supermemory` | SuperMemory | [supermemoryai/supermemory](https://github.com/supermemoryai/supermemory) | [supermemory.ai](https://supermemory.ai/) |
| 26 | `26-weaviate` | Weaviate | [weaviate/weaviate](https://github.com/weaviate/weaviate) | [weaviate.io](https://weaviate.io/) |
| 27 | `27-zep` | Zep | [getzep/zep](https://github.com/getzep/zep) | [getzep.com](https://www.getzep.com/) |

## Clone

Repos were cloned with `git clone --depth 1`. To refresh a framework:

```bash
cd <folder>
git pull
```

## Push to your GitHub

1. Create a new repository on GitHub (e.g. `survey-memory-integrate-pattern`), do **not** init with README.
2. Add remote and push:

```bash
git remote add origin https://github.com/YOUR_USERNAME/survey-memory-integrate-pattern.git
git push -u origin main
```

Replace `YOUR_USERNAME` with your GitHub username.

## License

Each subfolder is the upstream project; see its repository for license and terms.
